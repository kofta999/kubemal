apiVersion: v1
kind: Service
metadata:
  name: kubemal-service
  namespace: kubemal-ns
  labels:
    app: kubemal
spec:
  type: ClusterIP
  selector:
    app: kubemal
  ports:
    - name: http
      port: 443
      targetPort: 3000
      protocol: TCP
